# راهنمای دپلوی و دسترسی به وب‌سایت

## اطلاعات سرور (از پوشه ssh)

- **آدرس سرور:** `62.60.128.97`
- **کاربر:** `freelancer1`
- **پوشه پروژه روی سرور:** `/var/www/project1`
- **پسورد (در صورت نیاز):** طبق فایل `ssh/README_freelancer1.txt`

---

## روش دپلوی

### مرحله ۱: اجرای اسکریپت دپلوی (روی ویندوز)

در پوشه پروژه در PowerShell اجرا کنید:

```powershell
.\deploy.ps1
```

این اسکریپت:
1. فرانت‌اند را با Vite می‌سازد.
2. در صورت نبود، فایل `.env` را از `.env.production.example` می‌سازد.
3. پروژه را فشرده می‌کند (بدون db، venv، node_modules) و با SCP به سرور آپلود می‌کند.
4. روی سرور فایل را از حالت فشرده خارج می‌کند و اسکریپت نصب را اجرا می‌کند.

**اگر با کلید SSH وصل نمی‌شوید:** هنگام اجرا از شما پسورد خواسته می‌شود؛ پسورد را از `ssh/README_freelancer1.txt` وارد کنید.

### مرحله ۲: دپلوی دستی (در صورت خطا در اسکریپت)

1. **ساخت فرانت:**
   ```powershell
   cd frontend
   npm install
   npm run build
   ```

2. **اتصال به سرور:**
   ```powershell
   ssh freelancer1@62.60.128.97
   ```
   (در صورت درخواست، پسورد را وارد کنید.)

3. **آپلود فایل‌ها:** با WinSCP یا FileZilla پوشه‌های `backend` و `frontend` و `deploy` را در مسیر `/var/www/project1` روی سرور کپی کنید (یا از همان zip ساخته‌شده توسط اسکریپت استفاده کنید).

4. **روی سرور (Linux):**
   ```bash
   cd /var/www/project1
   chmod +x deploy/setup_on_server.sh
   ./deploy/setup_on_server.sh /var/www/project1
   ```

---

## اگر سایت در دسترس نیست

اگر بعد از دپلوی به آدرس بالا وصل نمی‌شوید، این اسکریپت را اجرا کنید تا پورت ۸۰۰۰ در فایروال باز شود و Gunicorn به‌صورت پایدار راه‌اندازی شود:

```powershell
python deploy/ensure_site_running.py
```

این اسکریپت: وضعیت Gunicorn و پورت را بررسی می‌کند، در صورت نیاز فایروال (ufw) را برای پورت ۸۰۰۰ باز می‌کند و Gunicorn را در حالت daemon دوباره راه‌اندازی می‌کند.

---

## خطاهای رایج و رفع آن‌ها

| خطا | علت احتمالی | کار انجام‌داده |
|-----|-------------|-----------------|
| `Permission denied (publickey,password)` | کلید SSH روی سرور برای این کاربر تنظیم نشده یا اشتباه است | با پسورد وصل شوید: `ssh freelancer1@62.60.128.97` و پسورد را وارد کنید. |
| `db.sqlite3 is being used` | فایل دیتابیس باز است | برنامه‌ای که از دیتابیس استفاده می‌کند (مثلاً runserver) را ببندید و دوباره اسکریپت را اجرا کنید. اسکریپت الان db را داخل zip قرار نمی‌دهد. |
| `No such file or directory` برای zip | ساخت zip خطا داده و فایل ساخته نشده | مطمئن شوید هیچ برنامه‌ای فایل‌های داخل `backend` را قفل نکرده. دپلوی دستی (مرحله ۲) را انجام دهید. |
| روی سرور: `python3: command not found` | Python روی سرور نصب نیست | روی سرور: `sudo apt-get update && sudo apt-get install -y python3 python3-pip python3-venv` |
| روی سرور: `frontend/dist` یافت نشد | فرانت روی سرور build نشده | حتماً روی ویندوز قبل از آپلود `npm run build` را در پوشه `frontend` اجرا کنید و پوشه `frontend/dist` را آپلود کنید. |

هر خطای دیگری که در زمان دپلوی می‌بینید را یادداشت کنید (متن کامل خطا و مرحله‌ای که رخ داده) تا بتوان مشکل را دقیق پیدا کرد.

---

## دسترسی به وب‌سایت

بعد از اجرای موفق اسکریپت نصب روی سرور (و در صورت باز بودن پورت ۸۰۰۰):

- **صفحه اصلی و اپلیکیشن:**  
  **http://62.60.128.97:8000**

- **پنل ادمین Django:**  
  **http://62.60.128.97:8000/admin/**

برای ساخت کاربر ادمین روی سرور یک‌بار اجرا کنید:

```bash
cd /var/www/project1/backend
source venv/bin/activate
python manage.py createsuperuser
```

اگر از مرورگر به آدرس بالا وصل نمی‌شوید، روی سرور پورت را باز کنید:

```bash
sudo ufw allow 8000
sudo ufw reload
```

برای اجرای دائمی (بعد از بستن SSH)، از `nohup` یا سرویس systemd برای gunicorn استفاده کنید؛ دستور پیشنهادی در خروجی اسکریپت `deploy/setup_on_server.sh` نمایش داده می‌شود.
